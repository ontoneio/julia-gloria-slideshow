---
import GridCard from './GridCard.astro'

function selectComponent(identity) {
    switch(identity) {
        case "grid":
            return GridCard
        default:
            
        } 
}
const { name } = Astro.props as Props;

const Component = selectComponent(name)
---
<Component />